skip_tags: true # avoid deploy to GitHub loop 

image: Visual Studio 2017

configuration:
#- Debug
- Release

before_build:
- nuget restore -Verbosity quiet

build:
  project: All.sln
  publish_nuget: true             # package projects with .nuspec files and push to artifacts
  publish_nuget_symbols: true     # generate and publish NuGet symbol packages
  include_nuget_references: true  # add -IncludeReferencedProjects option while packaging NuGet artifacts
  verbosity: minimal              # MSBuild verbosity level

after_test:
    nuget pack _Common\Zenseless.nuspec -symbols

artifacts:
- path: bin\$(configuration)\ShaderForm
  name: ShaderForm
- path: '*.nupkg'
  name: Zenseless
  
deploy:
  release: ShaderForm-v$(appveyor_build_version)
  description: 'Release auto generated from AppVeyor'
  provider: GitHub
  auth_token:
    secure: IGRhoLQi/yyC2ej7s79ZRU3PL3NScj4Kxh1Lyydg/CEvD/PbRwIilxmBziEHfwjJ
  artifact: ShaderForm
  draft: false
  prerelease: false
